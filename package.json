{
  "name": "monorepo+docker+ci_cd",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",

    "db:generate" : "cd packages/db && npx prisma generate && cd ../../",
    "start:web": "cd apps/web && pnpm run dev && cd ../../",
    "start:backend": "cd apps/http && pnpm run dev && cd ../../",
    "start:ws": "cd apps/ws && pnpm run dev && cd ../../"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.5",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
